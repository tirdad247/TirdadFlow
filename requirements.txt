# TirdadFlow runtime dependencies (minimum versions known to work)
# Note: scipy is NOT needed - WAV encoding uses the Python stdlib "wave" module.
PyQt6>=6.6
sounddevice>=0.4.6
numpy>=1.26
keyboard>=0.13.5
pyperclip>=1.8.2
groq>=0.11
httpx>=0.27

# Build-only (not needed to run from source):
# pyinstaller>=6.5
